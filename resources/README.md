My Example configuration files.

## Prerequisites

- Existing Example configuration files you want to keep should be backed up
- `~/.examplerc` and `~/.example/` should not exist

## Installation

    $ git clone https://github.com/jbrudvik/example.git
    $ example/install

## Updating

    $ example/update
