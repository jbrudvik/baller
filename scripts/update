#!/usr/bin/env bash

dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
hooks="${dir}/hooks"

pre="${hooks}/pre-update"
if [ -f "${pre}" ]; then
  source "${pre}"
fi

git -C "$(dirname "$0")" pull

post="${hooks}/post-update"
if [ -f "${post}" ]; then
  source "${post}"
fi
